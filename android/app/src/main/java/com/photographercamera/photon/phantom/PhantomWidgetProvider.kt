package com.photographercamera.photon.phantom

import android.appwidget.AppWidgetProvider

/**
 * 占位：上游 PhantomWidgetProvider 用于拍摄进度 Widget。本项目未启用幻影，
 * 仅保留类以兼容 CameraViewModel 的 ::class.java 引用（不实际生效）。
 */
class PhantomWidgetProvider : AppWidgetProvider()
