# 噪点 / 色彩断层 / 锐化痕迹 根因诊断与修复（1.3.3）

> 针对用户反馈：1.3.1 后，使用自有 profile（胶片人像）**JPEG MAX 仍有严重噪点和锐化痕迹**，**RAW MAX 噪点爆炸和摩尔纹未解决**。
> 用户假设：profile 的 JSON 用 sRGB 曲线，RAW 管线不适用 → 这是断层+噪点爆炸的原因。

## 结论先行

**用户的假设方向有道理，但机制描述与代码不符。** 经全链路核查：

- profile（recipe + 创意 3D LUT）**在两条链路都是 sRGB 显示空间应用**的，且共用同一个 `LutImageProcessor.performRender` 着色器。
- RAW 引擎（`RawDemosaicProcessor`）**根本不消费** profile 的 `ColorRecipeParams / FilmParamsStore / color_matrix / shadow_floor`（grep `raw/` 目录这些关键字零命中）。它只做 demosaic + 色调映射 + sRGB 编码 + 一条 **RAW 基线锐化(0.4)**。
- JPEG MAX 是 sRGB 路径、RAW MAX 也是先把 RAW 编码成 8-bit sRGB 再交给 `LutImageProcessor`。**两条链路 profile 应用点一致**——所以"JPEG MAX 也有噪点"恰恰证明：问题不是"线性空间套了 sRGB 曲线"，而是**profile 在 8-bit 已编码数据上做 sRGB 曲线重塑，把读出噪声/压缩噪声重新暴露出来**。

## 真实根因（三层）

### 1. sRGB 空间 profile 作用在 8-bit 已量化数据上（断层 + 噪点主因，两条链路都有）
- RAW MAX：`RawDemosaicProcessor.renderSrgbPass` 把线性 → 8-bit sRGB（`combinedTextureId`，1.3.1 已加 TPDF 抖动）→ 读成 Bitmap → `LutImageProcessor` 在 8-bit sRGB 上做 `film_curve.shadow_floor` 抬黑、`tone_curve` 重塑、`color_matrix` 蓝通道增益(1.118)。
- 抬黑在 sRGB 空间把原本"藏在陡峭 sRGB 暗部"的读出噪声抬成可见比例；曲线重塑让 8-bit 台阶重新分布 → 色带(断层)。
- JPEG MAX：输入是 8-bit JPEG，同样在 8-bit sRGB 上重塑 → 暴露 JPEG 压缩噪声。所以两条都有，但 RAW 更明显（纯净数据 + 全分辨率读出噪声）。

### 2. RAW 基线锐化 0.4 叠加在创意 profile 上（RAW 专属：锐化痕迹 + 摩尔纹 + 噪点爆炸）
- `RawDemosaicProcessor:3806` `renderFinalSharpenPass(actualMetadata, sharpeningValue, combinedOutput.encodedTextureId)` 在**最终 8-bit sRGB** 上做 0.4 锐化。
- `resolveCaptureSharpening` 对**所有 RAW 抓拍**都返回 0.4（胶片人像的 `sharpen=0` 被无视）。
- 结果：全分辨率 demosaic 的读出噪声 + 摩尔纹被 0.4 锐化放大 → **RAW MAX 比 JPEG MAX 更锐、更噪、有摩尔纹，两链路观感不一致**。

### 3. 1.3.1 的 shadow_floor 上限(10) 只缓解了胶片人像一种情况
- 1.3.1 把 `film_curve.shadow_floor` 从 25.5 压到 10，改善了胶片人像；但**并未改变"8-bit sRGB 上做曲线重塑"这一根本机制**，所以用户仍感到"严重噪点"。

## 1.3.3 已实施的修复（低风险、高收益）

### Fix A — RAW 基线锐化对创意 profile 关闭（治 RAW 锐化痕迹/摩尔纹/噪点爆炸 + 一致性）
`CameraViewModel.resolveCaptureSharpening` 增加 `lutId` 参数：创意 profile（`lutId != "none"`）激活时返回 `0f`，锐化改由 profile 的 `recipe.sharpness` 接管。
- 胶片人像(sharpen=0) → RAW 总锐化 = 0，与 JPEG MAX 一致，摩尔纹/噪点不再被放大。
- 带 sharpen 的 profile → 仍按 profile 自身值锐化，无功能损失。

### Fix B — `LutImageProcessor` 末级 TPDF 抖动（治两条链路的色彩断层）
在 `performRender` 最终输出前加 ±1 LSB TPDF 抖动（与 1.3.1 `RawSrgbPass` 同一算法），打散 profile 曲线重塑产生的 8-bit 量化台阶。RAW 编码抖动 + 此抖动双重覆盖，色带基本消除。

## 仍遗留 / 真正彻底的修复（下一里程碑，Option B）

**问题本质**：profile 是 sRGB 显示空间变换（profile 自身声明 `input/output_gamut=sRGB`，shadow_floor 单位是 8/255），在 sRGB 空间应用是"正确"的、**不该强行线性化**（否则破坏"所见即所得"的 Lookbook 一致性）。但当前它在 **8-bit 已编码** 数据上应用，才暴露噪声。

**正确做法（Option B，让 profile 同时干净适配 JPEG MAX 与 RAW MAX）**：
把 profile 的应用点从"8-bit sRGB Bitmap"改为"**16F 高比特工作空间**"——
- RAW MAX：在 RAW 引擎内，对线性 `engineToneTextureId` 先做 `linear→sRGB` 到 **RGBA16F**，再跑同一套 `LutImageProcessor` recipe/LUT 着色器（sRGB 语义不变），最后**只编码一次** 8-bit sRGB + 抖动。
- 好处：读出噪声在最终单次编码前保持低位，抬黑不再"重新暴露"噪声；16F 工作空间无 8-bit 色带；JPEG MAX 与 RAW MAX 共用同一线性/16F profile 数学 → **两链路像素级一致**。
- 代价：需把 recipe/LUT 着色器接入 RAW 引擎的 16F 纹理链（中等重构，需真机回归）。

**摩尔纹说明**：摩尔纹是 demosaic 混叠伪影，关掉 0.4 基线锐化只是"不再放大"，**不会消除**。彻底抑制需在 demosaic 阶段加轻度 AA / 去混叠（更大改动），建议作为独立项评估。

## 验证清单（待真机）
- [ ] RAW MAX + 胶片人像：摩尔纹/噪点是否被 0.4 关闭明显削弱
- [ ] JPEG MAX + 胶片人像：色彩断层是否消失（Fix B 抖动）
- [ ] RAW MAX vs JPEG MAX 暗部观感是否趋于一致
- [ ] 带 sharpen 的 profile：RAW 是否仍按 profile 值锐化（无回归）
